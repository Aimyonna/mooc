# ============================================================
# MOOC 课程信息采集系统 — 依赖清单
# 安装：pip install -r requirements.txt
# ============================================================

# 浏览器自动化
selenium>=4.15.0
webdriver-manager>=4.0.0

# HTTP 客户端（API 采集用）
requests>=2.31.0

# MySQL 数据库驱动
pymysql>=1.1.0

# 环境变量管理（.env 文件加载）
python-dotenv>=1.0.0
